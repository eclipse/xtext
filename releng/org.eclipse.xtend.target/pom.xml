<?xml version="1.0"?>
<project
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd"
	xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.eclipse.xtend</groupId>
		<artifactId>org.eclipse.xtend.tycho.parent</artifactId>
		<version>2.20.0-SNAPSHOT</version>
		<relativePath>../org.eclipse.xtend.tycho.parent</relativePath>
	</parent>

	<packaging>pom</packaging>
	<artifactId>org.eclipse.xtend.target</artifactId>

	<build>
		<plugins>
			<plugin>
				<groupId>org.codehaus.mojo</groupId>
				<artifactId>build-helper-maven-plugin</artifactId>
				<executions>
					<execution>
						<id>attach-artifacts</id>
						<phase>package</phase>
						<goals>
							<goal>attach-artifact</goal>
						</goals>
						<configuration>
							<artifacts>
								<artifact>
									<file>${project.artifactId}-oxygen.target</file>
									<type>target</type>
									<classifier>${project.artifactId}-oxygen</classifier>
								</artifact>
								<artifact>
									<file>${project.artifactId}-photon.target</file>
									<type>target</type>
									<classifier>${project.artifactId}-photon</classifier>
								</artifact>
								<artifact>
									<file>${project.artifactId}-r201809.target</file>
									<type>target</type>
									<classifier>${project.artifactId}-r201809</classifier>
								</artifact>
								<artifact>
									<file>${project.artifactId}-r201812.target</file>
									<type>target</type>
									<classifier>${project.artifactId}-r201812</classifier>
								</artifact>
								<artifact>
									<file>${project.artifactId}-r201903.target</file>
									<type>target</type>
									<classifier>${project.artifactId}-r201903</classifier>
								</artifact>
								<artifact>
									<file>${project.artifactId}-r201906.target</file>
									<type>target</type>
									<classifier>${project.artifactId}-r201906</classifier>
								</artifact>
								<artifact>
									<file>${project.artifactId}-r201909.target</file>
									<type>target</type>
									<classifier>${project.artifactId}-r201909</classifier>
								</artifact>
								<artifact>
									<file>${project.artifactId}-latest.target</file>
									<type>target</type>
									<classifier>${project.artifactId}-latest</classifier>
								</artifact>
							</artifacts>
						</configuration>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>
</project>

dependencies {
	testImplementation project(':org.eclipse.xtend.core.tests')
	testImplementation project(':org.eclipse.xtend.core.tests').sourceSets.test.output // TODO is there a better way
}

sourceSets.test {
	java.srcDir 'testdata'
	java.srcDir 'suites'
	resources.srcDir 'testdata'
}

test {
	maxHeapSize = "2G"
	exclude '**/*Suite.class'
}

dependencies {
	compile project(':org.eclipse.xtend.core.tests')
}

sourceSets.test {
	java.srcDir 'testdata'
	java.srcDir 'suites'
	resources.srcDir 'testdata'
}

test {
	maxHeapSize = "1500m"
	maxParallelForks = 1
	forkEvery = 100
	exclude '**/*Suite.class'
}
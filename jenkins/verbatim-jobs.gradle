file('verbatim').listFiles().findAll{it.name.startsWith('xtext-pull') || it.name.startsWith('buck')}.each { jobXml -> 
	jenkins.jobs.create(jobXml.name.replace('.xml', '')).definition {
		xml jobXml
	}
}
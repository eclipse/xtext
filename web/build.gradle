configure(subprojects.findAll{!it.name.contains('generator')}) {
	apply plugin: 'java'
	apply plugin: 'eclipse'
	sourceSets {
		main.java.srcDir 'src/main/xtend-gen'
		test.java.srcDir 'src/test/xtend-gen'
	}
}
/*
 * Although the operators in Xbase are syntactically predefined,
 * the semantic is giben by  a simple operator overloading mechanism.
 * 
 * Each operator maps to a certain method signature.
 */
 
val x = new helper.OperatorOverloaded()

x + 'y'
// x + y (String)
x .. 'y'
// x .. y (String)

